# copilotExample — starter personal website

This folder contains a small starter site:

- `index.html` — landing page
- `styles/` — CSS files
- `scripts/` — JavaScript modules
- `images/` — simple example SVG images
- `pages/` — linked site pages (about/projects/contact)

Open `index.html` in a browser to preview.
